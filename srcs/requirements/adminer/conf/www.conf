[www]
user = nobody
group = nobody

listen = 8000
listen.owner = nobody
listen.group = nobody
listen.mode = 0660

pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

clear_env = no

php_value[session.save_path] = /run/php/sessions
